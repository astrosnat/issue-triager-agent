# GitHub personal access token with repo scope (required)
GITHUB_TOKEN=your_personal_access_token

# Target repository to triage (optional, defaults to Azure-samples/azure-search-openai-demo)
TARGET_REPO=owner/repo-name
